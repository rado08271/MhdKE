<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="sk.rafig.mhdke">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".OopsActivity">
        </activity>
        <activity android:name=".ui.LegalActivity">
        </activity>
        <activity android:name=".ui.LoadingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ui.HistoryActivity">
        </activity>
        <activity android:name=".ui.TicketActivity">
        </activity>
        <activity android:name=".ui.AllowActivity">
        </activity>
        <activity android:name=".ui.WelcomeActivity">
        </activity>
    </application>

</manifest>